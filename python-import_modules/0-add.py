#!/usr/bin/python3
# Import the add function from add_0.py
from add_0 import add

# Assign the values 1 and 2 to variables a and b
a = 1
b = 2

# Call the add function with a and b as arguments and print the result
result = add(a, b)
print("{} + {} = {}".format(a, b, result))
